\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{float}

\geometry{margin=1in}

\title{Evolved Neural Controllers vs Hand-Designed Rules for Stigmergic Sorting}
\author{}
\date{}

\begin{document}

\maketitle

\begin{abstract}
% To be written last (M7)
\end{abstract}

\section{Introduction}

Collective behaviour in social insects presents a puzzle that has resisted simple explanation for over a century: how do individuals with no global view, no central coordinator, and no shared plan produce outcomes that look designed? A colony of ants sorting its brood into colour-coded piles seems to \emph{know} something that no single ant knows. The answer, as Grass\'e~\cite{grasse1959} observed, lies not in the agents but in the environment they leave behind --- each moved pellet, each deposited pheromone trace, becomes a signal that biases the next action. The work is distributed across time and space, embedded in the world itself. This is stigmergy.

What attracted us to this problem is not that it is well understood, but that it sits at an unresolved tension between two very different ways of engineering collective behaviour. On one side stand hand-designed rules like Deneubourg's probabilistic pickup/drop model~\cite{deneubourg1990}: elegant, minimal, provably effective for a narrow class of tasks. On the other stand evolved neural controllers: opaque, data-hungry, but capable of discovering strategies that no human would specify. The question is not which approach is ``better'' in the abstract --- it is which one works better \emph{here}, for this task, under these constraints, and what the answer tells us about the relationship between simplicity and learned complexity in collective systems.

We set out to test a specific hypothesis: that evolved neural controllers can discover sorting strategies that outperform the canonical Deneubourg baseline, but only under certain environmental conditions. Our intuition comes from two observations. First, the Deneubourg rule is remarkably efficient --- it sorts 200 pellets into coherent clusters using nothing but local similarity counts and two parameters. Beating it is not trivial. Second, the rule is also rigid --- it cannot adapt its behaviour based on what an agent is currently carrying, or modulate its exploration strategy in response to global progress. A neural controller with access to richer state information might exploit these blind spots, but only if the evolutionary budget is sufficient to discover the relevant structure.

We do not expect a single answer. If evolution beats the baseline, the finding is that learned controllers can extract value from state-dependent action selection in collective tasks --- a result that generalises to other swarm robotics problems. If the baseline holds or wins, the finding is equally interesting: that minimal hand-designed rules with good parameter choice remain highly efficient, and that evolution under tight computational budgets struggles to discover them. This would be a direct empirical line back to Brooks's argument for ``intelligence without representation''~\cite{brooks1991} --- that embodied, reactive controllers can outperform learned policies when the environment provides sufficient structure.

Either outcome is informative. The boundary between them --- the regime in which each approach dominates --- is the finding we are after.

\section{Background}
\label{sec:background}

\subsection{Stigmergic Sorting}
The term \emph{stigmergy} was coined by Grass\'e~\cite{grasse1959} to describe indirect coordination among social insects through environmental modifications. Workers leave traces (pheromones, moved objects) that stimulate subsequent actions by themselves or neighbours, producing emergent collective behaviour without central control.

Deneubourg et al.~\cite{deneubourg1990} formalised this into a computational sorting model: mobile agents pick up items probabilistically based on local similarity, carrying them until the surrounding environment matches the carried item's type. The pickup probability decreases with the number of similar neighbours (isolated items are preferred), while the drop probability increases with local similarity (items are deposited near matching clusters). Over time, this simple rule produces globally sorted configurations from random initial placement.

The model was motivated by observations of ant brood sorting~\cite{franks1992}, where worker ants organise nymphs into colour-coded piles using only local cues. This biological grounding makes stigmergic sorting a natural testbed for comparing hand-designed behavioural rules against evolved controllers: the task is simple enough that minimal rules succeed, yet complex enough that learned policies might discover superior strategies.

\subsection{Neuroevolution in Collective Tasks}
% To be written at M4

\section{Methods}
\label{sec:methods}

\subsection{Environment}
\label{sec:methods:environment}

We model a two-dimensional continuous arena of $100 \times 100$ units with hard-wall boundaries. The environment contains $200$ pellets---$100$ red and $100$ blue---placed uniformly at random. Twenty agents navigate the arena, each equipped with a circular sensor of radius $r=5$ that reports the count of pellets per colour within range.

The action space is discrete with five options: \textit{move} forward one unit in the current heading, \textit{turn left} ($-30^\circ$), \textit{turn right} ($+30^\circ$), \textit{pick up} a nearby pellet (within distance $1.0$), and \textit{drop} a carried pellet. Each agent can carry at most one pellet at a time.

We chose a $100 \times 100$ continuous arena rather than the $30 \times 30$ discrete grid used in the original Deneubourg formulation, because the larger space gives agents room for meaningful cluster formation without trivially saturating the environment. The discrete five-action space was selected over continuous heading control to keep the neural network output simple (five-way softmax) and the genetic algorithm search space well-behaved.

Simulation proceeds in synchronous discrete time: all agents compute actions based on the state at tick $t$, then all positions update simultaneously to $t\!+\!1$. Episodes run for at most $10{,}000$ ticks, with early termination if cluster purity plateaus for $2{,}000$ consecutive ticks ($\Delta < 0.01$).

All randomness is deterministic and reproducible. A master seed is split into independent subsystem streams (pellet placement, agent initialization, baseline stochasticity, genetic algorithm) via a \texttt{SeedBank} class. No global random state is used anywhere in the codebase.

\subsection{Baseline: Deneubourg Model}
\label{sec:methods:baseline}

Our baseline agent implements Deneubourg's probabilistic pickup/drop rules~\cite{deneubourg1990}. Each agent carries at most one pellet and decides its action each tick based on local sensor readings.

When \emph{not carrying} a pellet, the agent computes $f$ as the fraction of the dominant colour among pellets within sensor radius $r$. The probability of attempting pickup is:
\[
P_{\text{pickup}} = \left(\frac{k_1}{k_1 + f}\right)^2
\]
When $f$ is high (homogeneous neighbourhood), pickup is unlikely --- the agent avoids breaking up existing clusters. When $f$ is low (mixed neighbourhood), pickup is more likely. If pickup does not trigger, the agent performs a random walk (MOVE, TURN\_LEFT, or TURN\_RIGHT with equal probability).

When \emph{carrying} a pellet, the agent computes $f$ as the fraction of \emph{similar-coloured} pellets in the sensor window. The probability of dropping is:
\[
P_{\text{drop}} = \left(\frac{f}{k_2 + f}\right)^2
\]
High $f$ (many matching neighbours) increases drop probability --- the agent deposits the pellet near a matching cluster. Low $f$ suppresses dropping --- the agent continues searching. If drop does not trigger, the agent random-walks.

We use $k_1 = 0.1$ and $k_2 = 0.3$, matching the starting values recommended in the original formulation. Sensor radius $r = 5$ units. These parameters were chosen as conventional defaults before any tuning; they represent the ``off-the-shelf'' Deneubourg configuration against which evolved controllers are compared.

\subsection{Evaluation Metrics}
\label{sec:methods:metrics}

We use two complementary metrics to quantify sorting performance.

\paragraph{Cluster Purity.} Pellets are clustered using DBSCAN~\cite{ester1996} with $\varepsilon\!=\!5.0$ and \texttt{min\_samples}\texttt{=}2, matching the agent sensor radius. For each non-noise cluster, purity is the fraction of pellets sharing the most common colour. The overall score is the arithmetic mean across all non-noise clusters, yielding a value in $[0,1]$ where $1.0$ means every cluster is monochromatic.

\paragraph{Cluster Count.} The number of non-noise DBSCAN clusters. Lower values indicate more consolidation; however, cluster count alone is insufficient because an agent could achieve a low count by creating one large mixed cluster. Used alongside purity, it distinguishes ``many pure clusters'' from ``few impure ones''.

Both metrics were validated against three synthetic configurations: (i) perfectly sorted (purity\,=\,1.00, clusters\,=\,2), (ii) uniformly random (purity\,$\approx$\,0.76, clusters\,$\approx$\,41), and (iii) 80\% clustered with 20\% scattered noise (purity\,$\approx$\,0.78, clusters\,$\approx$\,7). All values fell within $\pm$5\% of expected bounds.

\subsection{Evolved Controller}
\label{sec:methods:evolved}

\paragraph{Neural Network Architecture.}
Each evolved agent is controlled by a single-layer feedforward neural network with 149 weights. The network takes a 6-dimensional input vector:
\[\mathbf{x} = [\text{red\_dens}, \text{blue\_dens}, \text{similar\_dens}, \text{dissimilar\_dens}, \text{carrying\_red}, \text{carrying\_blue}]\]
where density values are raw pellet counts within the sensor radius, and carrying flags are binary indicators of the carried pellet's colour. The hidden layer has 12 units with \(\tanh\) activation, and the output layer has 5 units with softmax activation, producing a probability distribution over the action space \(\{\text{MOVE}, \text{TURN\_LEFT}, \text{TURN\_RIGHT}, \text{PICKUP}, \text{DROP}\}\).

The architecture was chosen to balance expressiveness against search space tractability. A single hidden layer of 12 units provides enough capacity to learn non-trivial sensorimotor mappings (the universal approximation theorem guarantees any continuous function can be approximated with a single hidden layer~\cite{cybenko1989}), while keeping the genome size (149 weights) small enough for efficient genetic search. We deliberately avoided recurrent architectures (e.g., CTRNNs) despite their biological plausibility~\cite{trianni2002}, as the added complexity would require per-architecture hyperparameter tuning that confounds the baseline comparison.

\paragraph{Action Selection.}
Agents select actions greedily: the action with the highest softmax probability is executed. This deterministic policy ensures that fitness evaluations are reproducible (no stochastic variance from action sampling) and that the fitness landscape is smooth with respect to weight perturbations. We explored stochastic action selection (sampling from the softmax distribution) but found it produced a flat fitness landscape where the genetic algorithm could not discriminate between genomes~\cite{stanley2002}.

\paragraph{Genetic Algorithm.}
We evolve the 149 weight vectors directly using a simple genetic algorithm~\cite{holland1975}. The initial population of 30 genomes is generated with Xavier uniform initialization~\cite{glorot2010} for the weight matrices and zero initialization for biases. No pickup bias is applied (agents start with no innate tendency to pick up pellets), allowing the GA to discover whether pickup behaviour is beneficial.

Each generation proceeds as follows:
\begin{enumerate}
    \item The best individual from the previous generation is preserved (elitism).
    \item The remaining 29 individuals are produced by tournament selection (tournament size \(k\!=\!3\)) followed by per-gene Gaussian mutation (\(\sigma\!=\!0.3\)).
    \item No crossover is used, following the observation that mutation-only GAs can be effective for neural network weight evolution when the population size is sufficient~\cite{stanley2002}.
\end{enumerate}

\paragraph{Fitness Function.}
The fitness function is a weighted composite of five components, each normalised to \([0,1]\):
\[F = \alpha \cdot p_\text{term} + \beta \cdot p_\text{int} + \gamma \cdot c_\text{consol} + \delta_\text{eff} \cdot a_\text{act} + \varepsilon \cdot \ell_\text{local}\]

\textit{Terminal purity} (\(p_\text{term}\), \(\alpha\!=\!0.20\)): cluster purity at the end of the episode. The primary sorting objective, but weighted modestly because it is a sparse signal (agents must chain wander\(\to\)pickup\(\to\)drop behaviours before any signal exists).

\textit{Integrated purity} (\(p_\text{int}\), \(\beta\!=\!0.15\)): mean cluster purity sampled every 100 ticks. Provides denser gradient signal than terminal-only evaluation.

\textit{Consolidation} (\(c_\text{consol}\), \(\gamma\!=\!0.15\)): \(1 - (\text{final\_clusters} / \text{initial\_clusters})\). Rewards reduction in the number of DBSCAN clusters, encouraging agents to merge small clusters.

\textit{Activity} (\(a_\text{act}\), \(\delta\!=\!0.20\)): normalised count of pickup\(+\)drop events (capped at 400). Bootstrap reward that encourages agents to interact with pellets in early generations. The effective weight \(\delta_\text{eff}\) decays linearly to zero by generation 25: \(\delta_\text{eff} = \delta \cdot \max(0, 1 - g/25)\). This ensures activity is rewarded when the population needs it but does not dominate later generations.

\textit{Locality} (\(\ell_\text{local}\), \(\varepsilon\!=\!0.30\)): for each drop event, the fraction of nearby pellets (within sensor radius) that match the dropped pellet's colour, averaged over all drops. This is the highest-weighted component because it provides a per-action gradient: agents that drop near similar pellets receive immediate credit, even before global clusters consolidate. This component was added after observing that purity-only fitness produced a flat landscape where the GA could not improve~\cite{gross2011}.

\paragraph{Evaluation Protocol.}
Each genome is evaluated over \(k\!=\!3\) episodes with different random seeds (pellet placement varies, agent positions vary). The mean fitness across episodes is used for selection. Each episode runs for 5,000 ticks (half the full experiment length), balancing evaluation fidelity against computational cost. The master seed for the GA is independent from the experiment seeds, ensuring no data leakage.

\paragraph{Implementation Notes.}
The GA runs for 50 generations. With a population of 30, 3 evaluation episodes, and 5,000 ticks per episode, each generation requires \(29 \times 3 \times 5{,}000 = 435{,}000\) tick evaluations (the elite individual is not re-evaluated). The full evolution takes approximately 2 hours on a single CPU core. The best genome from generation 50 is used for the head-to-head comparison in Section~\ref{sec:results:main}.

\section{Results}
\label{sec:results}

\subsection{Experiment 1: Baseline vs Evolved}
\label{sec:results:main}

We compare the Deneubourg baseline against the best evolved neural controller (generation 50, fitness\,=\,0.7136) across 5 random seeds (42, 123, 256, 7, 999). Each condition runs for 10,000 ticks on identical arena configurations.

\paragraph{Headline result.} The Deneubourg baseline significantly outperforms the evolved controller on cluster purity. The baseline achieves a mean purity of 0.8337 ($\pm$0.0345) compared to 0.7193 ($\pm$0.0243) for the evolved controller --- a difference of 0.1144 (11.4\%). A Mann-Whitney U test confirms this difference is statistically significant ($U\!=\!25$, $p\!=\!0.0079$) with a rank-biserial effect size of $r\!=\!-1.0000$, indicating perfect separation: every baseline run outperforms every evolved run.

\begin{table}[H]
\centering
\begin{tabular}{llccc}
\toprule
Condition & $n$ & Mean & Std & Median \\
\midrule
Deneubourg & 5 & 0.8337 & 0.0345 & 0.8432 \\
Evolved & 5 & 0.7193 & 0.0243 & 0.7131 \\
\midrule
\textit{Mann-Whitney U} & -- & -- & $p\!=\!0.0079$ & $r\!=\!-1.0000$ \\
\bottomrule
\end{tabular}
\caption{Final cluster purity across 5 seeds. The Deneubourg baseline significantly outperforms the evolved neural controller ($p\!<\!0.01$).}
\label{tab:m5-results}
\end{table}

\paragraph{Cluster count.} Both conditions produce similar cluster counts (baseline: 42.6, evolved: 42.4), suggesting the evolved controller does not fail by producing too many or too few clusters, but rather by producing clusters of lower purity.

\paragraph{Purity over time.} The time-series comparison (Figure~\ref{fig:m5-timeseries}) shows that the baseline achieves higher purity earlier in the episode and maintains the lead throughout. The evolved controller shows a slower initial convergence rate, suggesting it struggles to discover effective pickup/drop strategies within the evolutionary budget.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/m5_boxplot.png}
\caption{Boxplot of final cluster purity across 5 seeds. The evolved controller shows no overlap with the baseline distribution.}
\label{fig:m5-boxplot}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/m5_timeseries.png}
\caption{Mean cluster purity over time (mean $\pm$ 1$\sigma$). The baseline achieves higher purity earlier and maintains the lead.}
\label{fig:m5-timeseries}
\end{figure}

\paragraph{Interpretation.} The evolved controller's underperformance suggests that the Deneubourg rule is highly efficient for this task --- a minimal hand-designed local rule with good parameter choice outperforms a learned policy under tight evolutionary budgets (50 generations, 30 population, 149 weights). This is consistent with Brooks's argument for ``intelligence without representation''~\cite{brooks1991}: when the environment provides sufficient structure (local similarity cues), simple reactive rules can outperform learned policies that must discover the relevant structure from scratch.

The evolved controller's fitness (0.7136) is a composite score that includes locality and activity bonuses not present in the purity metric. The GA may have optimised for these auxiliary components at the expense of terminal purity, suggesting a misalignment between the fitness function and the evaluation metric.

\subsection{Extensions}
\label{sec:results:extensions}
% To be written at M6

\section{Discussion}
\label{sec:discussion}
% To be written at M7

\section{Limitations}
\label{sec:limitations}
% To be written at M7

\section{Conclusion}
\label{sec:conclusion}
% To be written at M7

\section*{AI Declaration}
% To be written at M7

\section*{Development Log}
\label{sec:dev-log}
\textbf{PNG Snapshot Visualization (2026-05-24).} Added \texttt{render\_arena\_metadata()} to \texttt{viz.py} which renders arena scatter plots with a semi-transparent metadata overlay box (top-left, monospace). The overlay displays: seed, agent type, generation, tick, cluster purity (colour-coded: green $>0.85$, yellow $>0.75$, red otherwise), cluster count, pellets on ground, pellets carried, and wall time. Wired into \texttt{experiment.py} at tick 0, mid, and end snapshots. PNGs save alongside existing .txt snapshots in \texttt{docs/figures/}. Verified: sanity experiment produces paired \texttt{\{prefix\}\_tick\{N\}.txt} and \texttt{\{prefix\}\_tick\{N\}.png} files.

\textbf{Snapshot Render Script (2026-05-24).} Created \texttt{experiments/render\_snapshots.py} (70 lines) to convert all existing .txt snapshot files in \texttt{docs/figures/} to PNG scatter plots. CLI: \texttt{python experiments/render\_snapshots.py} renders all; single file path renders one. Uses matplotlib Agg backend with colour scheme matching \texttt{viz.py}. Generated 15 PNGs from existing snapshots.

\textbf{Pygame Live GUI (2026-05-24).} Created \texttt{src/gui.py} (292 lines) and \texttt{experiments/run\_gui.py} for real-time simulation visualization. 800$\times$800 window with dark background, HUD showing live metrics (purity, clusters, tick, ground, carried, pickups, drops, speed). Pellets rendered as coloured circles, agents as triangles pointing in heading direction. Controls: Space (pause), R (reset), Q (quit), +/- (speed 1x--10x), S (snapshot). Run: \texttt{python -m experiments.run\_gui --config experiments/configs/baseline.yaml}.

\textbf{Visualization Completion (2026-05-24).} All three visualization paths now ship with colour legends and DEBUG-GUIDE documentation for future AI agents. \texttt{viz.py} (303 lines): headless PNG rendering with metadata overlay + colour legend (upper-right). \texttt{render\_snapshots.py} (196 lines): converts existing .txt snapshots to PNGs with colour legend — 15 existing snapshots re-rendered. \texttt{gui.py} (330 lines): Pygame live GUI with \texttt{\_draw\_legend()} method showing pellet colours and agents in bottom-right corner. Colour map synchronization enforced: \texttt{\_PELLET\_COLOUR\_MAP} in \texttt{viz.py} is single source of truth; \texttt{gui.py} \texttt{PELLET\_COLOURS} (RGB tuples) and \texttt{render\_snapshots.py} \texttt{\_COLOURS} (hex strings) must match. All 41 tests pass.

\bibliographystyle{plain}
\bibliography{refs}

\end{document}
